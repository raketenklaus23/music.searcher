# === Phase 1: Fundament (Shell + Library + Basis-Analyse) ===
PySide6>=6.7
numpy>=1.26
librosa>=0.10.2
pyloudnorm>=0.1.1
soundfile>=0.12.1
mutagen>=1.47
scipy>=1.13
platformdirs>=4.0

# === Phase 2: Playback (Decks + Mixer) ===
sounddevice>=0.4.6
pyrubberband>=0.4.0
pedalboard>=0.9.0

# === Phase 4: Stems (heavy - CPU/GPU) ===
# demucs>=4.0.1
# torch>=2.2

# === Phase 5: Online Lookup ===
# reine stdlib (urllib) — kein extra Paket noetig

# === Phase 3: Struktur/Segmente (optional, msaf ist zickig unter Windows) ===
# msaf ist optional; librosa reicht für Basis-Segmentation

# === Phase 8: Standalone-Build ===
# pyinstaller>=6.0

# === Phase 9: MIDI (spaeter) ===
# python-rtmidi>=1.5
